name : test run command

on: [push]

jobs: 
  run-shell-command:
      runs-on: ubuntu-latest
      steps:
        - name: echo string
          run: echo "echo hello"
        - name: mulitline commands
          run:  |
            node -v
            npm -v
